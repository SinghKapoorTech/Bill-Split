/**
 * Single source of truth for the user-facing support address.
 *
 * Apple Guideline 1.5 requires the App Store "Support URL" to resolve to a page
 * offering a real way to reach the developer. The landing footer and the privacy
 * policy are both that route, so they must never drift apart — a support address
 * that differs between the two reads as an unmaintained listing to a reviewer.
 */
export const CONTACT_EMAIL = 'singhkapoortech@gmail.com';
